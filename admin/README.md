# Admin App

Admin and doctor dashboard React + Vite application for DocDock.

## Commands

```bash
npm install
npm run dev
npm run build
```

## Environment

Create `admin/.env`:

```env
VITE_API_URL=http://localhost:4000
```
