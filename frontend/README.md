# Frontend App

Patient-facing React + Vite application for DocDock.

## Commands

```bash
npm install
npm run dev
npm run build
```

## Environment

Create `frontend/.env`:

```env
VITE_API_URL=http://localhost:4000
```
